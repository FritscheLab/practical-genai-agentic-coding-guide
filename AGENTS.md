# AGENTS.md

This repository contains the online guide. The runnable exercise is at https://github.com/FritscheLab/practical-genai-agentic-coding-example.

Read README.md and the relevant lesson before editing. Keep the two language paths, relative links, lab theme, and Part 1 navigation intact. Use synthetic examples and copyable fenced prompts. Check provider claims against their official sources and preserve source review dates.

Keep VS Code with GitHub Copilot's Local Ask → Plan → Agent workflow as the default, including Explorer and visual Source Control review and local commits. Complete terminal workflows belong in the CLI appendix; optional clients retain their own controls. Apply `docs/reference/agent-control.md` when explaining approvals, sandboxing, independent coding, and readable repairs. Do not imply that repository instructions grant permissions or that shell checks establish tested UI steps. Prefer focused changes with a concrete need for any additional dependencies or abstractions.

Validate changes with `bundle exec jekyll build --strict_front_matter` and `python3 scripts/py/check_site.py _site`. Keep rendered output, dependencies, caches, and personal settings out of commits.
